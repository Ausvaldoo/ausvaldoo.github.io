# 依赖与构建产物
node_modules/
dist/
# 旧构建目录。构建前会把 dist 改名让开（某些受限环境里没法直接清空 dist），
# 留下的是 dist_old_<时间戳>。发布命令是 `git add -A`，不挡一下会被整包提交。
dist_old_*/

# VitePress 缓存与数据加载器临时文件
.vitepress/cache/
.vitepress/.temp/
*.timestamp-*.mjs

# Obsidian 工作区（配置含本机路径，且 workspace.json 变动频繁）
.obsidian/
.trash/

# Obsidian 粘贴到仓库根目录的孤儿图片：默认不入库。
# 要正式用图，请把文件放进 public/images/ 再用 ![](/images/xxx.png) 引用。
/Pasted image *.png

# AI 协作产生的项目记忆（.workbuddy/memory/*.md）。
# 里面是内部判断与决策过程，而这是公开仓库 —— 发布命令是 `git add -A`，不挡住会被整个提交上去。
.workbuddy/

# 历史工作区 _ws/（探针脚本 / 截图 / 视频 / 抓取样本，约 279MB）。
# 它是工具留档不是博客内容：里面的 .md 既不发布也不入库。
# 发布命令是 `git add -A`，不挡一下整包会被提交上去。
_ws/

# 交接与维护文档：不进公开仓库（站长 2026-09-26 决定）。
# 原因：这个仓库是**公开**的。AGENTS.md 不是网页，所以 srcExclude 管不到它 ——
# 一旦入库，任何人都能在 GitHub 上读到全文（含本机路径与内部决策过程）。
# ⚠️ 代价：克隆仓库的人拿不到它；CLAUDE.md 里的 @AGENTS.md 对克隆者失效。
# 文件仍在本机，本机所有窗口/AI 照常读得到。要备份请另找私有位置，别再加回 git。
AGENTS.md

# 站长的建站史档案（2026-09-25 那一轮写的，本来打算入库）。
# 2026-09-26 站长选择「留在自己电脑上」，故挡掉提交。
# 若要改回入库：删掉下面这一行即可（它已排除发布，不会变成网页）。
tools/建站史档案.md

# 系统杂项
Thumbs.db
.DS_Store
*.local
